name: test_prerequisites
on:
  workflow_dispatch:
  push:
    branches:
      - chore/update-claude-md-prerequisites

jobs:
  check_prerequisites:
    uses: ./.github/workflows/reusable_prerequisites.yml
